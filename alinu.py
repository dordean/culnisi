class Wrapper:
    def __init__(self, version):
        self.version = version

# Define the version
version = "1.0.0"

# Create an instance of Wrapper and set the version
wrapper = Wrapper(version)

print(wrapper.version)  # Output: "1.0.0"
